<!DOCTYPE html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Riddhi's Shortener URL Microservice</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="style.css" rel="stylesheet" type="text/css" />
</head>
<body>
    <div class="my-3 container text-center">
        <h1>Riddhi's <br> Shortener URL Microservice</h1>
        <div class="my-5 text-center">
            <form action="/" method="post">
                <div class="row text-center d-flex justify-content-center">
                    <div class="col-lg-8 col-md-8 col-sm-8 col-xs-8 col-8">
                        <input class="form-control form-control-lg" name="data" type="text"
                            placeholder="https://www.google.com" autocomplete="off" autofocus="true" required>
                    </div>
                </div>
                <button class="my-4 btn btn-dark" type="submit">Generate</button>
            </form>
        </div>
        <% if (locals.shortUrl) { %>
            <fieldset class="form custom-border">
                <legend >Your short URL</legend>
                <div class="form">
                    <h5 style="text-decoration: underline"><a class="d-inline-block ms-2" href="<%= shortUrl %>"><%= shortUrl %></a></h5>
                    <hr/>
                    <% if (locals.src) { %>
                        <img src="<%= src %>" alt="QR Code">
                    <% } %>
                </div>
            </fieldset>
            <% } %> 
    </div>
  <footer class="flex-wrap align-items-center py-3 my-4 text-center">
    <div>
      <span class="mb-3 mb-md-0 text-body-secondary align-items-center">Built by <span class="heart-symbol">&#x2764;</span> Riddhi Tailor</span>
    </div>
  </footer>
</body>
</html>
